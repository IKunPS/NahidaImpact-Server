syntax = "proto3";

// Nahida
// Proto

import "Reliquary.proto";
import "Weapon.proto";

message ShowEquip {
	uint32 item_id = 1;
	oneof detail {
		Reliquary reliquary = 2;
		Weapon weapon = 3;
	}
}
