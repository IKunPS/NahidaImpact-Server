syntax = "proto3";

// Nahida
// Proto

import "TowerTeam.proto";

message TowerCurLevelRecord {
	repeated TowerTeam tower_team_list = 6;
	repeated uint32 buff_id_list = 3;
	bool is_empty = 1;
	bool is_upper_part = 7;
	uint32 cur_floor_id = 14;
	uint32 cur_level_index = 10;
}
